# ─────────────────────────────────────────────────────────────
# Multi-Agent System for Automated Recruitment
# Requirements — Python 3.11
# Install with: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# ── Web Framework ─────────────────────────────────────────────
flask
werkzeug
blinker
click
itsdangerous
jinja2
MarkupSafe

# ── Environment ───────    ────────────────────────────────────────
python-dotenv

# ── LangChain Stack (pinned to compatible set) ────────────────
# Note: langchain-core 0.3.x is used with langchain-groq 0.3.x
# langchain is kept for langgraph 0.0.60 compatibility;
# installed with --no-deps to avoid resolver conflict.
langchain-core
langchain-groq
langchain-text-splitters
langsmith

# ── LangGraph ─────────────────────────────────────────────────
langgraph

# ── Groq / OpenAI SDKs ────────────────────────────────────────
groq
openai
langchain-openai

# ── PDF Parsing ───────────────────────────────────────────────
pymupdf

# ── HTTP & Async ──────────────────────────────────────────────
httpx
httpcore
h11
anyio
sniffio
aiohttp
aiosignal
aiohappyeyeballs
frozenlist
multidict
yarl
propcache

# ── Data Validation ───────────────────────────────────────────
pydantic
pydantic-core
annotated-types
typing-extensions
typing-inspect
mypy-extensions

# ── Serialization ─────────────────────────────────────────────
dataclasses-json
marshmallow
orjson
PyYAML
jsonpatch
jsonpointer

# ── Networking ────────────────────────────────────────────────
requests
requests-toolbelt
urllib3
certifi
charset-normalizer
idna

# ── Database (LangChain dependency) ───────────────────────────
sqlalchemy
greenlet

# ── Tokenisation ──────────────────────────────────────────────
tiktoken
regex

# ── Auth (JWT) ─────────────────────────────────────────────────
# Password hashing uses werkzeug.security (already a Flask dependency)
python-jose[cryptography]

# ── Utilities ─────────────────────────────────────────────────
colorama
numpy
tenacity
tqdm
packaging
distro
jiter
zstandard